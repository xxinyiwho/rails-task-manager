<h1><%= @task.title %></h1>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>

<a href=""><%= link_to 'back home', tasks_path %></a>
<p><%= link_to 'edit', edit_path %></p>
<p><%= link_to 'delete task',  destroy_path(@task), method: :delete, data: { confirm: 'are you sure?'} %></p>

